// New file: CallsView.swift
import SwiftUI

struct CallsView: View {
    var body: some View {
        PanelView(title: "CALLING") {
            VStack(alignment: .leading, spacing: 12) {
                Text("VOICE CALL UI COMING SOON")
                    .font(KairOSTypography.mono)
                    .foregroundStyle(KairOSColors.muted)
            }
        }
    }
}
